import { Paper } from '@mui/material';
import { styled } from '@mui/material/styles';

const PlacezPaper = styled(Paper)(({ theme }) => ({
  backgroundImage: 'none !important',
}));

export default PlacezPaper;
